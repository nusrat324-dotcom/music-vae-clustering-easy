# Music VAE Clustering

This project uses a Variational Autoencoder (VAE) to extract latent features from music data and perform clustering.

## Structure
- src/ : model and clustering code
- results/ : outputs, plots, metrics
